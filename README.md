# Mariners Game

Static landing page for [mariners-game.uk](https://mariners-game.uk), designed for GitHub Pages.

## Publish with GitHub Pages

In the repository settings, open **Pages**, choose **Deploy from a branch**, then select the `main` branch and the `/(root)` folder. Point the custom domain at `mariners-game.uk` and enable HTTPS once DNS has propagated.
